/* tslint:disable:no-console */
// @ts-nocheck
// This entrypoint existed only as a sandbox workaround (the host-mounted
// `dist/` directory could not be unlinked from inside the sandbox). It is
// intentionally a no-op now — use `./standalone.ts` as the canonical
// development entrypoint. Safe to delete at any time.
console.log('standalone-sandbox.ts is deprecated — use standalone.ts instead.')
